# Build output — note the trailing slash: this matches app/build too, not just ./build
build/
.gradle/
.cxx/
.externalNativeBuild/
/captures

# Local machine config (contains your SDK path)
local.properties
*.iml
.idea/

# Signing material — never commit these
*.jks
*.keystore
keystore.properties

# Artifacts
*.apk
*.aab
*.aar

# Sample media dropped in for debugging
*.mp4
*.m4a
*.webm
*.mkv

.DS_Store
Thumbs.db
